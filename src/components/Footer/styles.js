import { Box, Grid, styled, Link } from "@mui/material";

export const StyledOuterBox = styled(Box)({
  width: "100%",
  backgroundColor: "#232f3e",
  color: "white",
  textAlign: "left",
  mt: 5,
});
export const InnerGrid = styled(Grid)({
  textAlign: "center",
  backgroundColor: "#37475a",
  py: 2,
  
  
});
export const InnerLink = styled(Link)({
  textDecoration: "none",
  scrollBehavior: "smooth",
 
});
export const StyledGrid = styled(Grid)({
  backgroundColor: "#131a23",
  color: "#767676",
});
export const StyledInnerGrid = styled(Grid)({
  backgroundColor: "#131a23",
});
export const StyledAlignedGrid = styled(Grid)({
  backgroundColor: "#131a23",
  textAlign: "center",
  py: 2,
});
